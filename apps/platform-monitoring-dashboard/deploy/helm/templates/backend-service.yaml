apiVersion: v1
kind: Service
metadata:
  name: platform-monitoring-center-api
  namespace: {{ .Values.namespace.name }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: platform-monitoring-center-api
  ports:
    - name: http
      port: {{ .Values.backend.service.port }}
      targetPort: http
