{{- if .Values.namespace.create }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.namespace.name }}
  labels:
    app.kubernetes.io/name: platform-monitoring-center
    app.kubernetes.io/part-of: azure-aks-platform-blueprint
{{- end }}
