apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: currencyservice
  namespace: tenant-boutique-dev
spec:
  interval: 10m
  chartRef:
    kind: OCIRepository
    name: platform-app-service-chart
    namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    nameOverride: currencyservice
    replicaCount: 1
    image:
      repository: craksplatformsea001.azurecr.io/boutique/currencyservice
      tag: "0.1.0"
      pullPolicy: IfNotPresent
    platform:
      tenant: boutique
      app: boutique-app
      environment: dev
      keyVaultUrl: ""
      appSettings:
        enabled: true
        existingConfigMap: currencyservice-app-settings
        mountPath: /app/config/app-settings.json
    serviceAccount:
      create: true
      name: currencyservice
      workloadIdentity:
        enabled: true
        clientId: ""
    container:
      port: 7000
      env: {}
    service:
      enabled: true
      type: ClusterIP
      port: 80
    route:
      enabled: false
      gatewayName: shared-public
      gatewayNamespace: gateway-system
      hostnames:
        - boutique-app-dev.example.com
      pathPrefix: /
    nodeSelector:
      aks-platform.azure.com/node-purpose: app-workload
      aks-platform.azure.com/capacity: on-demand
    tolerations:
      - key: workload
        operator: Equal
        value: app-workload
        effect: NoSchedule
    topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: topology.kubernetes.io/zone
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels:
            app.kubernetes.io/instance: currencyservice

    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 300m
        memory: 256Mi
