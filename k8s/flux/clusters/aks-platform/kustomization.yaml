apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-sources.yaml
  # Re-enable observability after kube-prometheus-stack/loki tolerations are tuned
  # for the tainted demo node pools.
  # - ../observability
  # Re-enable these when returning to the full portfolio reconciliation graph.
  # - monitoring-dashboard.yaml
  # - node-provisioning.yaml
  - tenants.yaml
  - apps.yaml
