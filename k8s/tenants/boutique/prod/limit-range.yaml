apiVersion: v1
kind: LimitRange
metadata:
  name: default-container-limits
  namespace: tenant-boutique-prod
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 250m
        memory: 512Mi
      default:
        cpu: "1"
        memory: 2Gi
