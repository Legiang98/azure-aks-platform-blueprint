# Go Platform SDK

Go package skeleton for the AKS platform SDK.

It is intended to mirror the Node/Python SDK contract:

- Default Azure credential setup
- Key Vault secret access
- Application Insights event tracking
- Azure SQL Database connection strings using Microsoft Entra authentication

This package is a portfolio/demo skeleton. Publish to a private module registry or consume from a private repository for real use.
